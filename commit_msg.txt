🚀 Implement API versioning with /api/v1 base prefix

- Create v1 API router to group all API endpoints under /api/v1
- Keep health endpoints at root level (/health, /) for monitoring
- Update API endpoint URLs:
  • /verbs/* → /api/v1/verbs/*
  • /api-keys/* → /api/v1/api-keys/*
- Update OpenAPI documentation with new server URLs
- Update all tests to use new versioned endpoints
- All 25 verbs API tests passing with new URL structure ✅

Enables clean API versioning strategy for future development. 